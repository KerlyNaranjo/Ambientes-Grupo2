{% extends 'base.html.twig' %}

{% block body %}
    <div class="main container">
        <div class="row well">
            <div class="cold-md-6">
                <div class="page-header">
                    <h1>Cooperativa edit</h1>
                </div>


                {{ form_start(edit_form) }}
                <div class="form-group">
                    {{ form_label(edit_form.nombreCoop) }}
                    {{ form_widget(edit_form.nombreCoop, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.nombreCoop) }}</span>
                {#

                #}
                </div>
                <div class="form-group">
                    {{ form_label(edit_form.rucCoop) }}
                    {{ form_widget(edit_form.rucCoop, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.rucCoop) }}</span>

                </div>

                {#
                    {{ form_widget(edit_form) }}
                #}


                <input type="submit" value="Edit" />
                {{ form_end(edit_form) }}

                <ul>
                    <li>
                        <a href="{{ path('cooperativa_index') }}" class="btn btn-sm btn-info">Back to the list</a>
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Delete" class="btn btn-sm btn-danger btn-delete">
                        {{ form_end(delete_form) }}
                    </li>
                </ul>
            </div>

        </div>

    </div>
{% endblock %}