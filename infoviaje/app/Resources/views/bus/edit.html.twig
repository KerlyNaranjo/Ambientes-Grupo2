{% extends 'base.html.twig' %}

{% block body %}

    <div class="main container">
        <div class="row well">
            <div class="cold-md-6">
                <div class="page-header">
                    <h1>Bus edit</h1>
                </div>

                {{ form_start(edit_form) }}
                <div class="form-group">
                    {{ form_label(edit_form.matricula) }}
                    {{ form_widget(edit_form.matricula, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.matricula) }}</span>
                    {#

                    #}
                </div>
                <div class="form-group">
                    {{ form_label(edit_form.capacidad) }}
                    {{ form_widget(edit_form.capacidad, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.capacidad) }}</span>

                </div>

                <div class="form-group">
                    {{ form_label(edit_form.descripcionBus) }}
                    {{ form_widget(edit_form.descripcionBus, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.descripcionBus) }}</span>

                </div>

                <div class="form-group">
                    {{ form_label(edit_form.nomChofer) }}
                    {{ form_widget(edit_form.nomChofer, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.nomChofer) }}</span>

                </div>

                <div class="form-group">
                    {{ form_label(edit_form.nomAyudante) }}
                    {{ form_widget(edit_form.nomAyudante, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.nomAyudante) }}</span>

                </div>

                <div class="form-group">
                    {{ form_label(edit_form.cooperativa) }}
                    {{ form_widget(edit_form.cooperativa, { 'attr' : {'class' : 'form-control'} }) }}
                    <span class="text-danger">{{ form_errors(edit_form.cooperativa) }}</span>

                </div>




{#
{{ form_widget(edit_form) }}
#}

        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('bus_index') }}" class="btn btn-sm btn-info">Back to the list</a>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-sm btn-danger btn-delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
            </div>

        </div>

    </div>

{% endblock %}
