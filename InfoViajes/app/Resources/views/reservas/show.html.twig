{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reserva</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reserva.id }}</td>
            </tr>
            <tr>
                <th>Numeroasiento</th>
                <td>{{ reserva.numeroAsiento }}</td>
            </tr>
            <tr>
                <th>Fechareserva</th>
                <td>{% if reserva.fechaReserva %}{{ reserva.fechaReserva|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('reservas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('reservas_edit', { 'id': reserva.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
