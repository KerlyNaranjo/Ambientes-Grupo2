{% extends 'menu.html.twig' %}

{% block title %}Infoviajes{% endblock %}

{% block body %}
    {{ parent() }}
<div class="container">
    <div class="cold-md-12">
        <div class="page-header margin-none">
            <h1>Lista de cooperativas</h1>
        </div>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cooperativaa</th>
                <th>RUC</th>
                <th>Acción</th>
            </tr>
        </thead>
        <tbody>
        {% for cooperativa in cooperativas %}
            <tr>
                <td><a href="{{ path('cooperativa_show', { 'id': cooperativa.id }) }}">{{ cooperativa.id }}</a></td>
                <td>{{ cooperativa.nombreCooperativa }}</td>
                <td>{{ cooperativa.rucCooperativa }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('cooperativa_show', { 'id': cooperativa.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('cooperativa_edit', { 'id': cooperativa.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cooperativa_new') }}">Crear nueva cooperativa</a>
        </li>
    </ul>
        </div>
    </div>
</div>
{% endblock %}
